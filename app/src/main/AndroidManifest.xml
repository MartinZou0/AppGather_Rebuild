<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.martinzou.appgather_rebuild">


    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/apps_logo"
        android:label="@string/appname"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.FindPassword">

        </activity>
        <activity android:name=".activity.SecurityCode">

        </activity>
        <activity android:name=".activity.Maininterface">

        </activity>

        <activity android:name=".activity.Register">

        </activity>

        <activity android:name=".activity.AppStore">

        </activity>

        <activity android:name=".activity.PersonalCenter">

        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

</manifest>